codecov:
 notify:
    after_n_builds: 10

comment:
  after_n_builds: 10

coverage:
  status:
    project:
      acceptance:
        flags:
          - acceptance
      unit:
        after_n_builds: 1
        flags:
          - unit
